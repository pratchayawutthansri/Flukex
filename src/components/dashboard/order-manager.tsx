"use client";

import { useMemo, useState } from "react";
import { Clock3, Search, ShoppingBag } from "lucide-react";
import { PageHeader } from "./page-header";
import { Card } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import type { OrderStatus } from "@/domain/types";
import { formatCurrency, formatDateTime } from "@/lib/utils";
import { useDemoStore } from "@/store/demo-store";
const variants:Record<OrderStatus,"warning"|"success"|"danger"|"secondary"|"outline">={WAITING:"warning",PREPARING:"warning",READY:"success",SERVED:"secondary",CANCELLED:"danger"};
export function OrderManager(){const orders=useDemoStore(s=>s.orders);const update=useDemoStore(s=>s.updateOrderStatus);const[search,setSearch]=useState("");const[status,setStatus]=useState("ALL");const filtered=useMemo(()=>orders.filter(o=>(status==="ALL"||o.status===status)&&`${o.orderNumber} ${o.tableName}`.toLowerCase().includes(search.toLowerCase())),[orders,status,search]);return <><PageHeader title="ออเดอร์" description="ติดตามออเดอร์จาก POS และ QR Ordering ในที่เดียว"/><div className="mb-5 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><Input className="pl-10" placeholder="ค้นหาเลขออเดอร์หรือโต๊ะ..." value={search} onChange={e=>setSearch(e.target.value)}/></div><select aria-label="กรองสถานะ" className="min-h-11 rounded-lg border bg-card px-3 text-sm" value={status} onChange={e=>setStatus(e.target.value)}><option value="ALL">ทุกสถานะ</option>{Object.keys(variants).map(item=><option key={item}>{item}</option>)}</select></div>{filtered.length?<div className="space-y-4">{filtered.map(order=><Card key={order.id} className="p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div className="flex gap-3"><span className="grid size-11 shrink-0 place-items-center rounded-xl bg-secondary text-primary"><ShoppingBag/></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="text-lg font-bold">{order.orderNumber}</h2><Badge variant={order.source==="QR"?"default":"outline"}>{order.source}</Badge><Badge variant={variants[order.status]}>{order.status}</Badge></div><p className="mt-1 text-sm text-muted-foreground">{order.tableName} • {formatDateTime(order.createdAt)}</p></div></div><div className="sm:text-right"><p className="text-lg font-bold text-primary">{formatCurrency(order.totals.grandTotal)}</p><p className="text-xs text-muted-foreground">{order.items.reduce((sum,item)=>sum+item.quantity,0)} รายการ</p></div></div><div className="mt-4 border-t pt-4"><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{order.items.map(item=><div key={item.id} className="rounded-lg bg-muted p-3"><div className="flex justify-between gap-2 text-sm"><span><strong>{item.quantity}×</strong> {item.productName}</span><Badge variant={variants[item.status]}>{item.status}</Badge></div>{item.note&&<p className="mt-1 text-xs text-warning">หมายเหตุ: {item.note}</p>}</div>)}</div><div className="mt-4 flex flex-wrap items-center justify-between gap-3"><span className="flex items-center gap-2 text-xs text-muted-foreground"><Clock3 className="size-4"/>อัปเดต {formatDateTime(order.updatedAt)}</span><select aria-label={`เปลี่ยนสถานะ ${order.orderNumber}`} className="min-h-10 rounded-lg border bg-card px-3 text-xs" value={order.status} onChange={e=>update(order.id,null,e.target.value as OrderStatus)}>{Object.keys(variants).map(item=><option key={item}>{item}</option>)}</select></div></div></Card>)}</div>:<div className="grid min-h-64 place-items-center rounded-xl border border-dashed bg-card"><div className="text-center"><ShoppingBag className="mx-auto size-10 text-muted-foreground"/><h2 className="mt-3 font-bold">ไม่พบออเดอร์</h2><p className="text-sm text-muted-foreground">ลองเปลี่ยนตัวกรองหรือคำค้นหา</p></div></div>}</>}
