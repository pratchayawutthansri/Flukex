"use client";

import Link from "next/link";
import { ArrowUpRight, Banknote, ChefHat, CircleDollarSign, ClipboardList, QrCode, ShoppingBag, Sparkles, Users } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { PageHeader } from "./page-header";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Progress } from "@/components/ui/progress";
import { getEntitlements, SUBSCRIPTION_PLANS } from "@/config/plans";
import { formatCurrency, formatTime } from "@/lib/utils";
import { getCurrentUsage, useDemoStore } from "@/store/demo-store";

const sales = [{ day: "จ.", value: 18200 }, { day: "อ.", value: 21800 }, { day: "พ.", value: 19600 }, { day: "พฤ.", value: 24400 }, { day: "ศ.", value: 28900 }, { day: "ส.", value: 34200 }, { day: "อา.", value: 24860 }];

export function DashboardOverview() {
  const state = useDemoStore();
  const usage = getCurrentUsage(state);
  const limits = getEntitlements(state.planId);
  const paidOrders = state.orders.filter((order) => order.paidAt);
  const todaySales = 24860 + paidOrders.reduce((sum, order) => sum + order.totals.grandTotal, 0);
  const activeTables = state.tables.filter((table) => table.status !== "AVAILABLE" && table.status !== "DISABLED").length;
  const usageRows = [{ label: "สาขา", value: usage.branches, max: limits.maxBranches }, { label: "ผู้ใช้", value: usage.users, max: limits.maxUsers }, { label: "โต๊ะ", value: usage.tables, max: limits.maxTables }, { label: "สินค้า", value: usage.products, max: limits.maxProducts }, { label: "ออเดอร์เดือนนี้", value: usage.monthlyOrders, max: limits.monthlyOrderLimit }];
  return <><PageHeader title="ภาพรวมร้านวันนี้" description="ข้อมูลเดโมอัปเดตจาก Local Storage และพร้อม sync ข้ามแท็บผ่าน BroadcastChannel" actions={<><Button variant="outline" asChild><Link href="/order/demo-restaurant/table/table-01"><QrCode/>เปิด QR Demo</Link></Button><Button asChild><Link href="/pos"><ShoppingBag/>เปิด POS</Link></Button></>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[{label:"ยอดขายวันนี้",value:formatCurrency(todaySales),note:"+12.5% จากเมื่อวาน",icon:CircleDollarSign,color:"text-success"},{label:"ออเดอร์วันนี้",value:String(86+state.orders.length),note:`${state.orders.filter(o=>o.status!=="SERVED").length} กำลังดำเนินการ`,icon:ClipboardList,color:"text-primary"},{label:"โต๊ะที่ใช้งาน",value:`${activeTables} / ${state.tables.length}`,note:"อัปเดตจาก POS และ QR",icon:Users,color:"text-accent"},{label:"ยอดเฉลี่ย/บิล",value:formatCurrency(428),note:"+฿32 สัปดาห์นี้",icon:Banknote,color:"text-blue-600"}].map((metric) => <Card key={metric.label}><CardContent className="p-5"><div className="flex items-start justify-between"><div><p className="text-sm text-muted-foreground">{metric.label}</p><p className="mt-2 text-2xl font-bold">{metric.value}</p><p className={`mt-2 text-xs font-semibold ${metric.color}`}>{metric.note}</p></div><span className="grid size-11 place-items-center rounded-xl bg-muted"><metric.icon className={`size-5 ${metric.color}`}/></span></div></CardContent></Card>)}</div><div className="mt-5 grid gap-5 xl:grid-cols-[1.6fr_1fr]"><Card><CardHeader className="flex-row items-center justify-between"><div><CardTitle>ยอดขาย 7 วัน</CardTitle><p className="mt-1 text-xs text-muted-foreground">หน่วย: บาท • ทุกช่องทาง</p></div><Badge variant="success"><ArrowUpRight className="size-3"/>8.4%</Badge></CardHeader><CardContent><div className="h-72 w-full" role="img" aria-label="กราฟยอดขาย 7 วัน"><ResponsiveContainer width="100%" height="100%"><AreaChart data={sales} margin={{top:8,right:8,left:-16,bottom:0}}><defs><linearGradient id="sales-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#dc2626" stopOpacity={0.35}/><stop offset="100%" stopColor="#dc2626" stopOpacity={0.02}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="var(--border)"/><XAxis dataKey="day" tickLine={false} axisLine={false} fontSize={12}/><YAxis tickLine={false} axisLine={false} fontSize={11} tickFormatter={(v)=>`${v/1000}k`}/><Tooltip formatter={(value)=>formatCurrency(Number(value))} contentStyle={{borderRadius:12,border:"1px solid var(--border)",background:"var(--card)"}}/><Area type="monotone" dataKey="value" stroke="#dc2626" strokeWidth={3} fill="url(#sales-fill)"/></AreaChart></ResponsiveContainer></div></CardContent></Card><Card><CardHeader><div className="flex items-center justify-between"><div><CardTitle>การใช้งานแพ็กเกจ</CardTitle><p className="mt-1 text-xs text-muted-foreground">แผน {SUBSCRIPTION_PLANS[state.planId].name}</p></div><Sparkles className="size-5 text-accent"/></div></CardHeader><CardContent className="space-y-4">{usageRows.map((row) => { const pct = Math.min(100,row.value/row.max*100); return <div key={row.label}><div className="mb-1.5 flex justify-between text-xs"><span>{row.label}</span><span className="font-semibold">{row.value.toLocaleString("th-TH")} / {row.max.toLocaleString("th-TH")}</span></div><Progress value={pct}/></div>; })}<Button variant="outline" className="w-full" asChild><Link href="/dashboard/subscription">จัดการแพ็กเกจ</Link></Button></CardContent></Card></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.4fr_1fr]"><Card><CardHeader className="flex-row items-center justify-between"><CardTitle>ออเดอร์ล่าสุด</CardTitle><Button variant="ghost" size="sm" asChild><Link href="/dashboard/orders">ดูทั้งหมด</Link></Button></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-sm"><thead><tr className="border-b text-left text-xs text-muted-foreground"><th className="pb-3">ออเดอร์</th><th className="pb-3">โต๊ะ</th><th className="pb-3">เวลา</th><th className="pb-3">สถานะ</th><th className="pb-3 text-right">ยอดรวม</th></tr></thead><tbody>{state.orders.slice(0,5).map((order)=><tr key={order.id} className="border-b last:border-0"><td className="py-3 font-bold">{order.orderNumber}</td><td>{order.tableName}</td><td>{formatTime(order.createdAt)}</td><td><Badge variant={order.status==="READY"?"success":order.status==="PREPARING"?"warning":"secondary"}>{order.status}</Badge></td><td className="text-right font-semibold">{formatCurrency(order.totals.grandTotal)}</td></tr>)}</tbody></table></div></CardContent></Card><Card><CardHeader><CardTitle>สินค้าขายดี</CardTitle></CardHeader><CardContent className="space-y-4">{[{name:"กะเพราเนื้อวากิว",qty:48,pct:94},{name:"ผัดไทยกุ้งสด",qty:37,pct:76},{name:"ชาไทยครีมสด",qty:29,pct:61},{name:"ต้มยำกุ้งน้ำข้น",qty:21,pct:48}].map((product,index)=><div key={product.name} className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-lg bg-secondary text-xs font-bold text-primary">{index+1}</span><div className="min-w-0 flex-1"><div className="flex justify-between gap-2 text-sm"><span className="truncate font-medium">{product.name}</span><span className="text-muted-foreground">{product.qty} จาน</span></div><Progress value={product.pct} className="mt-1.5 h-1.5"/></div></div>)}</CardContent></Card></div><div className="mt-5 rounded-xl border bg-[#4a0d0d] p-5 text-white"><div className="flex flex-col items-start justify-between gap-4 sm:flex-row sm:items-center"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-white/10"><ChefHat/></span><div><p className="font-bold">จอครัวมี {state.orders.filter(o=>o.items.some(i=>i.station==="KITCHEN"&&!["SERVED","CANCELLED"].includes(i.status))).length} ออเดอร์ที่กำลังทำ</p><p className="text-xs text-red-100/70">เปิดจอ KDS เพื่ออัปเดตสถานะรายการ</p></div></div><Button variant="accent" asChild><Link href="/kitchen">เปิดจอครัว</Link></Button></div></div></>;
}
