import Link from "next/link";
import { ArrowLeft, ShieldCheck } from "lucide-react";
import { BrandLogo } from "@/components/brand-logo";

export function AuthShell({ title, description, children }: { title: string; description: string; children: React.ReactNode }) {
  return <main id="main-content" className="mesh-bg min-h-dvh px-4 py-6 sm:px-6"><div className="mx-auto flex max-w-6xl items-center justify-between"><BrandLogo/><Link href="/" className="inline-flex min-h-11 items-center gap-2 rounded-lg px-3 text-sm font-semibold text-muted-foreground hover:bg-card"><ArrowLeft className="size-4"/>กลับหน้าหลัก</Link></div><div className="mx-auto grid max-w-6xl items-center gap-12 py-10 lg:min-h-[calc(100dvh-90px)] lg:grid-cols-[0.9fr_1.1fr]"><div className="hidden lg:block"><div className="max-w-lg"><span className="grid size-14 place-items-center rounded-2xl bg-primary text-white shadow-lg"><ShieldCheck className="size-7"/></span><h1 className="mt-6 text-balance text-4xl font-bold">{title}</h1><p className="mt-4 text-lg text-muted-foreground">{description}</p><div className="mt-8 rounded-xl border bg-card/75 p-5 text-sm shadow-soft"><strong>ดูแลข้อมูลอย่างเป็นระบบ</strong><p className="mt-2 text-muted-foreground">สิทธิ์การใช้งานแยกตามบทบาทและร้านค้า พร้อมควบคุมการเข้าถึงข้อมูลสำคัญ</p></div></div></div><div className="mx-auto w-full max-w-xl rounded-2xl border bg-card p-6 shadow-2xl sm:p-8"><div className="mb-6 lg:hidden"><h1 className="text-3xl font-bold">{title}</h1><p className="mt-2 text-sm text-muted-foreground">{description}</p></div>{children}</div></div></main>;
}
